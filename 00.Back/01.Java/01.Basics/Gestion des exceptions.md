https://docs.oracle.com/javase/7/docs/api/java/lang/Exception.html?is-external=true
List de toutes les exceptions existantes


Checked vs unchecked :

- On utilise les checked exception quand le try catch est utile
- le Try catch est utile si le catch permet d’une façon ou d'une autre de gérer le problème ( exemple : database de backup)

Pour le reste unchecked 

Kotlin fonctionne sur la base de unchecked